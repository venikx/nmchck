{
  "name": "nmchck",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*", "infra"],
  "scripts": {
    "build": "npm run build --workspace frontend && npm run build --workspace lambda-function",
    "deploy": "npm run deploy --workspace infra"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "dependencies": {},
  "engines": {
    "npm": ">=10.8.0",
    "node": ">=20.0.0"
  }
}
